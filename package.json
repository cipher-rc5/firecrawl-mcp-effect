{
  "name": "firecrawl-mcp-effect",
  "version": "0.1.0",
  "description": "Firecrawl MCP server — self-hostable, Vercel-compatible, Effect-TS",
  "type": "module",
  "packageManager": "bun@1.3.10",
  "bin": {
    "firecrawl-mcp": "./src/stdio-server.ts"
  },
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "stdio": "bun run src/stdio-server.ts",
    "build": "bun build api/mcp.ts api/health.ts --outdir dist --target node --format esm --external @mendable/firecrawl-js --external effect --external @effect/platform",
    "typecheck": "bun tsc --noEmit",
    "smoke": "bun run scripts/smoke.ts",
    "fmt": "dprint fmt",
    "fmt:check": "dprint check",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "vercel-build": "bun run typecheck && bun run build"
  },
  "dependencies": {
    "@effect/platform": "0.96.0",
    "@mendable/firecrawl-js": "4.18.1",
    "effect": "3.21.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "dprint": "0.53.2",
    "typescript": "6.0.2"
  },
  "engines": {
    "bun": "1.3.10"
  }
}
